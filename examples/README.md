# 小型示例

这里用于存放聚焦单一知识点、可以独立运行的示例，例如：

- 自定义 Provider 与注入 Token
- 动态模块
- 自定义参数装饰器
- WebSocket Gateway
- 微服务 Transport
- 队列、缓存和定时任务

基础概念优先在 `demo/` 中演示。只有当某个能力需要额外基础设施、会让主 Demo 变得难以理解，或适合多种实现对比时，才放到这里。
